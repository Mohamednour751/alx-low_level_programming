#include <stdio.h>
/**
 * main - Entry point
 * description: A C progam that prints with printf fnction
 * Return: ALways 0 (Success)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
